/*
* Copyright(C), 2007-2008, XUPT Univ.
* ????????TTMS_UC_01		 
* File name: Studio_Persist.c			  
* Description : ???????????????	
* Author:   XUPT  		 
* Version:  v.1 	 
* Date: 	2015??4??22??	
*/

#include "Studio_Persist.h"
#include "../Service/Studio.h"
#include "EntityKey_Persist.h"	 
#include "../Common/list.h"
#include <stdlib.h>
#include <stdio.h>
#include<unistd.h>
#include <assert.h>


static const char STUDIO_DATA_FILE[] = "Studio.dat"; //?????????????? 
static const char STUDIO_DATA_TEMP_FILE[] = "StudioTmp.dat"; //????????????????? 
static const char STUDIO_KEY_NAME[] = "Studio"; //??????????? 

/*
???????TTMS_SCU_Studio_Perst_Insert
???????????????????????????????????????
?????????data?studio_t????????????????????????????
?? ?? ?????????????????????????????????
*/ 
int Studio_Perst_Insert(studio_t *data) {	 
	assert(NULL!=data);

	////??????????????????????????
	////??????????????????????????
	long key = EntKey_Perst_GetNewKeys(STUDIO_KEY_NAME, 1); //??????????????
	if(key<=0)			//??????????????????
		return 0;
	data->id = key;		//?????????????UI??
	////??????????????????????????
	////??????????????????????????



	FILE *fp = fopen(STUDIO_DATA_FILE, "ab");
	int rtn = 0;
	if (NULL == fp) {
		printf("Cannot open file %s!\n", STUDIO_DATA_FILE);
		return 0;
	}

	rtn = fwrite(data, sizeof(studio_t), 1, fp);

	fclose(fp);
	return rtn;
}

/*
???????TTMS_SCU_Studio_Perst_Update 
???????????????????§Ú????????????????
?????????data?studio_t????????????????????????????
?? ?? ?????????????????????????????????
*/
int Studio_Perst_Update(const studio_t * data) {
	assert(NULL!=data);

	FILE *fp = fopen(STUDIO_DATA_FILE, "rb+");
	if (NULL == fp) {
		printf("Cannot open file %s!\n", STUDIO_DATA_FILE);
		return 0;
	}

	studio_t buf;
	int found = 0;

	while (!feof(fp)) {
		if (fread(&buf, sizeof(studio_t), 1, fp)) {
			if (buf.id == data->id) {
				fseek(fp, -((int)sizeof(studio_t)), SEEK_CUR);
				fwrite(data, sizeof(studio_t), 1, fp);
				found = 1;
				break;
			}

		}
	}
	fclose(fp);

	return found;
}

/*
???????TTMS_SCU_Studio_Perst_DeleteByID
?????????????????????????????????????
??????????????????ID??????????????????????????ID???????????buf?studio_t???????????????????????
?? ?? ????????????????????????????????
*/
int Studio_Perst_DeleteByID(int ID) {

	//????????????????????????????§Õ??????????§µ????????????????????

	//?????????????????
	if(rename(STUDIO_DATA_FILE, STUDIO_DATA_TEMP_FILE)<0)//¿¿¿¿¿¿,¿¿¿ 0 ,¿¿¿¿ -1
	{
		printf("Cannot open file %s!\n", STUDIO_DATA_FILE);
		return 0;
	}

	FILE *fpSour, *fpTarg;  
	fpSour = fopen(STUDIO_DATA_TEMP_FILE, "rb");  //¿¿¿¿¿¿¿¿¿¿,¿¿NULL
	if (NULL == fpSour ){
		printf("Cannot open file %s!\n", STUDIO_DATA_FILE);
		return 0;
	}

	fpTarg = fopen(STUDIO_DATA_FILE, "wb");   
	if ( NULL == fpTarg ) {
		printf("Cannot open file %s!\n", STUDIO_DATA_TEMP_FILE);
		return 0;
	}


	studio_t buf;  //¿¿¿

	int found = 0;
	while (!feof(fpSour)) 
	{   //feof  ¿¿¿¿¿¿¿¿¿¿¿0¿¿¿¿¿¿0
		if (fread(&buf, sizeof(studio_t), 1, fpSour)) //¿¿¿¿¿¿¿,¿¿ size ¿¿¿ ¿¿¿¿count ¿¿,¿fpsour¿¿
		{
			if (ID == buf.id) 
			{
				found = 1;
				continue;
			}
			fwrite(&buf, sizeof(studio_t), 1, fpTarg);
		}
	}

	fclose(fpTarg);
	fclose(fpSour);

	//?????????
	remove(STUDIO_DATA_TEMP_FILE);
	return found;
}

/*
???????TTMS_SCU_Studio_Perst_SelectByID 
??????????????????????????????????????
??????????????????ID??????????????????????????ID???????????buf?studio_t???????????????????????
?? ?? ?????????????????????????????????
*/
int Studio_Perst_SelectByID(int ID, studio_t *buf) {
	assert(NULL!=buf);

	FILE *fp = fopen(STUDIO_DATA_FILE, "rb");
	if (NULL == fp) {
		return 0;
	}

	studio_t data;
	int found = 0;

	while (!feof(fp)) {
		if (fread(&data, sizeof(studio_t), 1, fp)) {
			if (ID == data.id) {
				*buf = data;
				found = 1;
				break;
			};

		}
	}
	fclose(fp);

	return found;
}

/*
???????TTMS_SCU_Studio_Perst_SelAll 
?????????????????????????????????????
?????????list??studio_list_t?????????????????????????
?? ?? ????????????????????????????????
*/
int Studio_Perst_SelectAll(studio_list_t list) {
	studio_node_t *newNode;
	studio_t data;
	int recCount = 0;

	assert(NULL!=list);

	List_Free(list, studio_node_t);

	FILE *fp = fopen(STUDIO_DATA_FILE, "rb");
	if (NULL == fp) { //?????????
		return 0;
	}

	while (!feof(fp)) {
		if (fread(&data, sizeof(studio_t), 1, fp)) {
			newNode = (studio_node_t*) malloc(sizeof(studio_node_t));
			if (!newNode) {
				printf(
						"Warning, Memory OverFlow!!!\n Cannot Load more Data into memory!!!\n");
				break;
			}
			newNode->data = data;
			List_AddTail(list, newNode);
			recCount++;
		}
	}
	fclose(fp);
	return recCount;
}
